/**
 * @param {string} s
 * @param {number} x
 * @param {number} y
 * @return {number}
 */
var maximumGain = function(s, x, y) {
  const higher=x>y?"ab":"ba"
  const lower=x<y?"ab":"ba"
   while(s.indexOf) 
};

maximumGain("cdbcbbaaabab",4,5)
